{
  programs.nvf.settings.vim = {
    binds = {
      cheatsheet.enable = true;
      whichKey = {
        enable = true;
        setupOpts = {
          preset = "helix";
        };
      };
    };
  };
}
